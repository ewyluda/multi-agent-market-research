"""Seth Klarman — Margin of safety, distressed assets, patient capital deployment."""

from .base_council_agent import BaseCouncilAgent


class KlarmanAgent(BaseCouncilAgent):
    INVESTOR_KEY = "klarman"
