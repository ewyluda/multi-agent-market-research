"""Benjamin Graham — Security analysis, margin of safety, value trap detection."""

from .base_council_agent import BaseCouncilAgent


class GrahamAgent(BaseCouncilAgent):
    INVESTOR_KEY = "graham"
